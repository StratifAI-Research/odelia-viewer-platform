# NOTE: `--extra-index-url` cannot be expressed in pyproject.toml; this file
# is the canonical install path when an index URL matters. python-pin-check
# skips `-`-prefixed lines, so the version pins below are still enforced.
#
# ODV-214 decision: the vendored MediSwarm models (mediswarm/) are validated
# against MediSwarm's stack (numpy<2 / monai 1.4 / torch 2.2). This service is
# an independent serving container and therefore pins that OLDER generation,
# not the numpy-2 pins used by the viewer/orthanc code. Keep in lockstep with
# pyproject.toml. Versions below mirror docker_config/Dockerfile_ODELIA.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- MediSwarm model stack (must match the vendored models) ---
torch==2.2.2
torchvision==0.17.2
numpy==1.26.4
monai==1.4.0
pytorch-lightning==2.4.0
torchmetrics==1.7.1
torchio==0.20.1
einops==0.8.0
x-transformers==2.3.5
timm==1.0.15
# nnU-Net residual encoder blocks for the DivideAndConquer challenge model.
dynamic-network-architectures==0.4.4

# --- serving / DICOM stack ---
Flask==3.1.3
Flask-Cors==6.0.2
huggingface-hub==0.29.3
pydicom==3.0.1
nibabel==5.3.2
requests==2.32.3
dicomweb-client==0.61.0
Pillow==11.0.0
scipy==1.15.3
SimpleITK==2.4.0
