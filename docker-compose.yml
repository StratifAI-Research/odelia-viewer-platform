version: '3.8'

services:
  # OHIF Viewer service (main viewer)
  viewer:
    build:
      context: ../../
      dockerfile: Dockerfile
    image: ${DOCKER_HUB_USERNAME:-stratifai}/odelia-viewer:${TAG:-latest}
    container_name: odelia-viewer
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf
      - ./config/app-config.js:/usr/share/nginx/html/app-config.js
 #     - ./logs/nginx:/var/log/nginx
    ports:
      - '8081:8081'
    depends_on:
      - orthanc-viewer
      - keycloak
    networks:
      - odelia-network
    environment:
      - PUBLIC_URL=/
      - PORT=8081
    user: "nginx:nginx"

  # Orthanc Viewer service (for OHIF to connect to)
  orthanc-viewer:
    build:
      context: ./orthanc-routing-example/orthanc-viewer
      dockerfile: Dockerfile
    image: ${DOCKER_HUB_USERNAME:-stratifai}/odelia-orthanc-viewer:${TAG:-latest}
    hostname: orthanc-viewer
    container_name: odelia-orthanc-viewer
    volumes:
      - ./orthanc-routing-example/orthanc-viewer/orthanc.json:/etc/orthanc/orthanc.json:ro
      - ./orthanc-routing-example/orthanc-viewer/router.py:/python/router.py:ro
      - ./orthanc-routing-example/orthanc-viewer/feedback_routes.py:/python/feedback_routes.py:ro
      - ./orthanc-routing-example/orthanc-viewer/feedback_db.py:/python/feedback_db.py:ro
      - ./orthanc-routing-example/orthanc-viewer/ups:/python/ups:ro
      - ./volumes/orthanc-viewer-db/:/var/lib/orthanc/db/
      - ./volumes/feedback-db/:/var/lib/odelia-feedback/
    restart: unless-stopped
    depends_on:
      - keycloak
    networks:
      - odelia-network
    ports:
      - '8000:8042' # HTTP port
      - '2000:4242' # DICOM port
    environment:
      - ORTHANC__NAME=orthanc-viewer
      - VERBOSE_ENABLED=true
      - VERBOSE_STARTUP=true
      - ORTHANC__PYTHON_SCRIPT=/python/router.py
      - ORTHANC__PYTHON_VERBOSE=true
      - ORTHANC_FEEDBACK_DB_DIR=/var/lib/odelia-feedback
      - ORTHANC_FEEDBACK_ENABLE_WAL=1

  # Orthanc Router service for MST AI model
  orthanc-router-mst:
    build:
      context: ./orthanc-routing-example/orthanc-router
      dockerfile: Dockerfile
    image: ${DOCKER_HUB_USERNAME:-stratifai}/odelia-orthanc-router:${TAG:-latest}
    hostname: orthanc-router-mst
    container_name: odelia-orthanc-router-mst
    volumes:
      - ./config/orthanc-router.json:/etc/orthanc/orthanc.json:ro
      - ./volumes/orthanc-router-mst-db/:/var/lib/orthanc/db/
      - ./orthanc-routing-example/orthanc-router/server.py:/python/server.py
      - ./orthanc-routing-example/MLIntegration/MST-classification/manifest.json:/etc/orthanc/manifest.json:ro
    restart: unless-stopped
    networks:
      - odelia-network
    ports:
      - '4243:4242' # DICOM port (avoid conflict)
      - '8043:8042' # HTTP port (avoid conflict)
    environment:
      - ORTHANC__NAME=orthanc-router-mst
      - VERBOSE_ENABLED=true
      - VERBOSE_STARTUP=true
      - ORTHANC__PYTHON_SCRIPT=/python/server.py
      - ORTHANC__PYTHON_VERBOSE=true
      - MODEL_BACKEND_URL=http://mst-classifier:5556
    depends_on:
      - mst-classifier

  # MST Classification Model
  mst-classifier:
    build:
      context: ./orthanc-routing-example/MLIntegration
      dockerfile: MST-classification/Dockerfile
    image: ${DOCKER_HUB_USERNAME:-stratifai}/odelia-mst-classifier:${TAG:-latest}
    container_name: odelia-mst-classifier
    environment:
      ORTHANC_URL: "http://orthanc-router-mst:8042"
      IMAGE_FOLDER: "/app/mst-service/images"
      MODEL_PATH: "/app/mst-service/mst_model"
    networks:
      - odelia-network
    ports:
      - '5556:5556'

  # Orthanc Router service for MedGemma AI model
  orthanc-router-medgemma:
    build:
      context: ./orthanc-routing-example/orthanc-router
      dockerfile: Dockerfile
    image: ${DOCKER_HUB_USERNAME:-stratifai}/odelia-orthanc-router:${TAG:-latest}
    hostname: orthanc-router-medgemma
    container_name: odelia-orthanc-router-medgemma
    volumes:
      - ./config/orthanc-router.json:/etc/orthanc/orthanc.json:ro
      - ./volumes/orthanc-router-medgemma-db/:/var/lib/orthanc/db/
      - ./orthanc-routing-example/orthanc-router/server.py:/python/server.py
    restart: unless-stopped
    networks:
      - odelia-network
    ports:
      - '4244:4242' # DICOM port (avoid conflict)
      - '8044:8042' # HTTP port (avoid conflict)
    environment:
      - ORTHANC__NAME=orthanc-router-medgemma
      - VERBOSE_ENABLED=true
      - VERBOSE_STARTUP=true
      - ORTHANC__PYTHON_SCRIPT=/python/server.py
      - ORTHANC__PYTHON_VERBOSE=true
      - MODEL_BACKEND_URL=http://medgemma-mri:5557
      - AI_NAME=MedGemma Vision-Language Model
    depends_on:
      - medgemma-mri

  # MedGemma MRI Classification Model
  medgemma-mri:
    build:
      context: ./orthanc-routing-example/MLIntegration
      dockerfile: medgemma-mri/Dockerfile
    image: ${DOCKER_HUB_USERNAME:-stratifai}/odelia-medgemma-mri:${TAG:-latest}
    container_name: odelia-medgemma-mri
    environment:
      ORTHANC_URL: "http://orthanc-router-medgemma:8042"
      IMAGE_FOLDER: "/app/medgemma-service/images"
      HF_TOKEN: "HF_TOKEN"
      NUM_SLICES: "100"
      MAX_NEW_TOKENS: "500"
      TORCH_DTYPE: "bfloat16"
    networks:
      - odelia-network
    ports:
      - '5557:5557'
#    deploy:
#      resources:
#        reservations:
#          devices:
#            - driver: nvidia
#              count: 1
#              capabilities: [gpu]

  # Chat Middleware - WebSocket-based chat for DICOM study analysis
  # Supports two backends: Ollama (default) and llama.cpp (optional).
  # Override via .env or shell: BACKEND_TYPE=llamacpp OLLAMA_URL=http://llamacpp-server:8090
  chat-middleware:
    build:
      context: ./orthanc-routing-example/MLIntegration
      dockerfile: chat-middleware/Dockerfile
    image: ${DOCKER_HUB_USERNAME:-stratifai}/odelia-chat-middleware:${TAG:-latest}
    container_name: odelia-chat-middleware
    environment:
      OLLAMA_URL: "${OLLAMA_URL:-http://host.docker.internal:11434}"
      OLLAMA_MODEL: "${OLLAMA_MODEL:-thiagomoraes/medgemma-1.5-4b-it:F16}"
      BACKEND_TYPE: "${BACKEND_TYPE:-ollama}"
      OLLAMA_NUM_CTX: "128000"  # 128k context window
      WADO_BASE_URL: "http://orthanc-viewer:8042/dicom-web"
      NUM_SLICES: "5"
      MAX_CACHE_ENTRIES: "100"
    networks:
      - odelia-network
    ports:
      - '5560:5560'
    depends_on:
      - orthanc-viewer
    # Enable host.docker.internal on Linux
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # llama.cpp server - optional alternative to Ollama for the Chat AI.
  # Only starts when the "llamacpp" profile is activated:
  #   docker compose --profile llamacpp up -d llamacpp-server chat-middleware
  # Requires GGUF model files in ./volumes/models/ (see README for download instructions).
  llamacpp-server:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    profiles: [llamacpp]
    container_name: odelia-llamacpp-server
    volumes:
      - ./volumes/models:/models:ro
    ports:
      - '8090:8090'
    networks:
      - odelia-network
    restart: unless-stopped
    command:
      - --host
      - "0.0.0.0"
      - --port
      - "8090"
      - -m
      - /models/${GGUF_MODEL_FILE:-medgemma-1.5-4b-it-BF16.gguf}
      - --mmproj
      - /models/${MMPROJ_FILE:-mmproj-BF16.gguf}
      - -c
      - "${LLAMA_CTX_SIZE:-131072}"
      - -ngl
      - "${LLAMA_N_GPU_LAYERS:-99}"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]

  # Grafana for feedback analytics (reads SQLite via plugin)
  grafana:
    image: grafana/grafana:11.1.0
    container_name: odelia-grafana
    ports:
      - '3000:3000'
    user: "0:0"                          # run as root
    environment:
      - GF_INSTALL_PLUGINS=frser-sqlite-datasource
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=odelia
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./volumes/feedback-db/:/var/lib/odelia-feedback/
    depends_on:
      - orthanc-viewer
    networks:
      - odelia-network

  keycloak:
    image: quay.io/keycloak/keycloak:24.0.5
    command: 'start-dev --import-realm'
    hostname: keycloak
    container_name: keycloak
    volumes:
      - ./config/ohif-keycloak-realm.json:/opt/keycloak/data/import/ohif-keycloak-realm.json
    environment:
      # Database
      KC_DB_URL_HOST: postgres
      KC_DB: postgres
      KC_DB_URL: 'jdbc:postgresql://postgres:5432/keycloak'
      KC_DB_SCHEMA: public
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: password
      KC_HOSTNAME_ADMIN_URL: http://localhost:8081/keycloak/
      KC_HOSTNAME_URL: http://localhost:8081/keycloak/
      KC_HOSTNAME_STRICT_BACKCHANNEL: "true"
      KC_HOSTNAME_STRICT_HTTPS: "false"
      KC_HTTP_ENABLED: "true"
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_HEALTH_ENABLED: "true"
      KC_METRICS_ENABLED: "true"
      KC_PROXY: edge
      KC_PROXY_HEADERS: xforwarded
      KEYCLOAK_JDBC_PARAMS: connectTimeout=40000
      KC_LOG_LEVEL: INFO
      KC_HOSTNAME_DEBUG: "true"
      # added later
      PROXY_ADDRESS_FORWARDING: "true"
    ports:
      - 8080:8080
    depends_on:
      - postgres
    restart: unless-stopped
    networks:
      - odelia-network
    healthcheck:
      test: ["CMD-SHELL", "exec 3<>/dev/tcp/127.0.0.1/8080;echo -e \"GET /health/ready HTTP/1.1\r\nhost: http://localhost\r\nConnection: close\r\n\r\n\" >&3;grep \"HTTP/1.1 200 OK\" <&3"]
      interval: 1s
      timeout: 5s
      retries: 10
      start_period: 60s

  postgres:
    image: postgres:15
    hostname: postgres
    container_name: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: password
    restart: unless-stopped
    networks:
      - odelia-network

volumes:
  postgres_data:
    driver: local

networks:
  odelia-network:
    driver: bridge
