pytest==9.0.3
pytest-asyncio==1.4.0
requests==2.34.2
jq==1.6.0
# Runtime deps imported at module top-level by the smoke tests (and future unit tests)
numpy==2.2.6
pydicom==3.0.2
Pillow==12.2.0
dicomweb-client==0.61.0
pandas==2.3.3
# MLIntegration service deps (Flask from MST/medgemma-mri; fastapi+pydantic from chat-middleware)
Flask==3.1.3
Flask-Cors==6.0.2
fastapi==0.136.1
httpx==0.28.1
pydantic==2.13.4
aiohttp==3.14.1
# Real torch / torchio / SimpleITK for MLI service unit tests (BC + MST + medgemma)
# CPU-only wheels keep CI install size manageable.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
torchio==1.2.0
SimpleITK==2.5.5
# odelia-classification built-in model deps (ODV-214). NB: these are the CI /
# numpy-2 generation (monai 1.6), NOT the service's runtime pin (monai 1.4);
# the model block is verified working on both stacks.
monai==1.6.0
pytorch-lightning==2.6.5
torchmetrics==1.9.0
x-transformers==2.19.6
einops==0.8.2
# Challenge-model backbones so CI can build+forward the whole roster.
torchvision==0.27.0
timm==1.0.15
dynamic-network-architectures==0.4.4
